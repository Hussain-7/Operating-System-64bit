    mov si,Message
    mov ax,0xb800
    mov es,ax
    xor di,di
    mov cx,MessageLen

PrintMessage:
    mov al,[si]
    mov [es:di],al   ;storing character
    mov byte[es:di+1],0xa  ;storing background and foreground color

    add di,2   
    add si,1
    ;This checks if cx is zero if not decrements and gos to print message label
    loop PrintMessage     